{
  "name": "repolens-ai-server",
  "version": "1.0.0",
  "description": "Backend server for RepoLens AI — explains GitHub repos using AI",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.js",
    "start": "node src/index.js",
    "build": "cd ../client && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.0",
    "express": "^4.21.0"
  }
}
